## 🚀 시작하기

### Backend 실행
```bash
cd server
npm run dev
```

### Frontend 실행
```bash
cd client
npm start

## host관련오류가 날 경우
$Env:DANGEROUSLY_DISABLE_HOST_CHECK = 'true'
npm start
```


### 자주사용하는 명령어
```bash
netstat -ano|findstr :5000
taskkill /PID 142496 /F
```

### Frontend package 재설치
```bash
Remove-Item -Recurse -Force node_modules
Remove-Item package-lock.json
npm cache clean --force

npm install
npm start
```